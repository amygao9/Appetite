import React from 'react';
import {StyleSheet, View, Image, Text} from 'react-native';
import {LongButton, Container} from "../../components";

const Auth = ({navigation}) => {
    return (
        <Container>
            <Text>Hello</Text>
            <LongButton title="hello" />
        </Container>
    )
}

export default Auth; 